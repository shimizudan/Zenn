---
title: "Juliaで複素数変換を可視化：f(z) = z(z+1)の美しい曲線を描く"
emoji: "🌀"
type: "idea" # tech: 技術記事 / idea: アイデア
topics: [julia, 複素数, 数学, 可視化, CairoMakie]
published: false
---

## はじめに

複素数関数による変換は、複素解析の中でも特に視覚的に興味深いテーマです。本記事では、Juliaを使って複素数関数 **f(z) = z(z+1)** による平面の変換を可視化し、直交格子がどのように曲線格子へと変換されるかを探ります。

CairoMakieを使った美しい可視化により、複素数変換の幾何学的な性質を直感的に理解できます。

## 複素数変換 f(z) = z(z+1) とは

### 関数の定義

複素数 **z = x + yi** に対して、以下の変換を考えます：

$$
f(z) = z(z+1) = z^2 + z
$$

この関数は、複素平面上の各点を新しい位置へ写します。

### 実部と虚部の分解

z = x + yi を代入して展開すると：

$$
\begin{aligned}
f(z) &= (x + yi)^2 + (x + yi) \\
&= (x^2 - y^2) + 2xyi + x + yi \\
&= (x^2 - y^2 + x) + i(2xy + y)
\end{aligned}
$$

したがって、変換後の複素数 **w = u + vi** の実部と虚部は：

$$
\begin{cases}
u = x^2 - y^2 + x \\
v = 2xy + y = y(2x + 1)
\end{cases}
$$

### ヤコビ行列

変換 f(z) = z² + z の実関数表現 (x, y) → (u, v) のヤコビ行列を求めます：

$$
J = \begin{pmatrix}
\frac{\partial u}{\partial x} & \frac{\partial u}{\partial y} \\
\frac{\partial v}{\partial x} & \frac{\partial v}{\partial y}
\end{pmatrix}
$$

u = x² - y² + x と v = 2xy + y から：

$$
J = \begin{pmatrix}
2x + 1 & -2y \\
2y & 2x + 1
\end{pmatrix}
$$

ヤコビ行列の行列式（ヤコビアン）は：

$$
\det(J) = (2x + 1)^2 + 4y^2
$$

これは複素微分 f'(z) = 2z + 1 の絶対値の2乗 |f'(z)|² に等しくなります：

$$
|f'(z)|^2 = |2z + 1|^2 = (2x + 1)^2 + 4y^2 = \det(J)
$$

### 同相写像と微分同相写像

変換の可逆性を理解するために、まず同相写像と微分同相写像の概念を整理します。

#### 同相写像（Homeomorphism）

写像 f: X → Y が**同相写像**であるとは：

1. **全単射**: f は1対1対応
2. **連続**: f が連続写像
3. **逆写像も連続**: f⁻¹ も連続写像

同相写像は「位相的に同じ形」を保つ変換です（例：円と楕円は同相、球面とドーナツは同相でない）。

#### 微分同相写像（Diffeomorphism）

写像 f: X → Y が**微分同相写像**であるとは：

1. **全単射**
2. **f が微分可能**（C¹級以上）
3. **f⁻¹ も微分可能**
4. **各点で Df が可逆**（det(Df) ≠ 0）

微分同相写像は同相写像よりも強い条件で、微分可能性を保ちながら可逆である変換です。

#### 局所微分同相写像

点 z₀ で f が**局所微分同相**であるとは、z₀ の十分小さな近傍で f が微分同相写像になることです。これが成り立つ条件は**逆関数定理**により：

$$
\det(J) \neq 0 \quad \text{at } z_0
$$

つまり、ヤコビ行列が正則であれば局所的に可逆な変換となります。

### Df が可逆でない点（臨界点）

ヤコビ行列 J が正則でない点、すなわち det(J) = 0 となる点を求めます：

$$
\det(J) = (2x + 1)^2 + 4y^2 = 0
$$

実数 x, y に対して、この等式が成り立つのは：

$$
\begin{cases}
(2x + 1)^2 = 0 \\
4y^2 = 0
\end{cases}
\quad \Longrightarrow \quad
\begin{cases}
x = -\frac{1}{2} \\
y = 0
\end{cases}
$$

したがって、**z = -1/2** が唯一の臨界点です。この点では：

- f'(-1/2) = 2(-1/2) + 1 = 0
- det(J) = 0
- 変換 f は局所的に可逆ではない（局所微分同相でない）

複素平面全体で、Df が可逆でない点は **z = -1/2 のみ**です。

#### 臨界点での幾何学的な振る舞い

det(J) = 0 となる臨界点では、変換が局所的に以下のような性質を持ちます：

- **情報の損失**: 十分小さな円が線分や点に退化する可能性
- **分岐**: 複数の異なる点が同じ像に写される
- **等角性の破綻**: 角度が保たれなくなる

一方、det(J) ≠ 0 の点では：

- **局所的な可逆性**: 逆写像が存在
- **等角写像**: 角度が保たれる（正則関数の性質）
- **面積の比例**: det(J) が局所的な面積の拡大率を表す

### 変換の性質

この変換には以下の特徴があります：

1. **非線形変換**: 直線が曲線に変換される
2. **2次関数**: z² の項により、平面が折り畳まれるような変換が生じる
3. **特異点**: z = -1/2 で f'(z) = 0 となり、det(J) = 0（臨界点）
   - この点で局所的な向きが保たれなくなる
4. **等角写像**: 臨界点以外では det(J) > 0 であり、局所的に角度を保つ

特に、直交座標の格子（x = 定数、y = 定数の直線群）がどのような曲線に変換されるかを可視化することで、この変換の性質が明らかになります。

## 可視化コード

変換前後の格子を並べて表示するJuliaコードを以下に示します。

```julia
using CairoMakie
using LinearAlgebra

# Mac標準の日本語フォントを設定
set_theme!(fonts = (regular = "Hiragino Sans", bold = "Hiragino Sans"))

# 複素数変換関数
f(z) = z * (z + 1)

# 格子の範囲設定
x_range = -3:0.5:3
y_range = -3:0.5:3

# 変換前の格子データ生成
function create_grid_lines(x_range, y_range)
    vertical_lines = []
    horizontal_lines = []

    # 垂直線（x = 定数）
    for x in x_range
        line_x = Float64[]
        line_y = Float64[]
        for y in range(minimum(y_range), maximum(y_range), length=100)
            push!(line_x, x)
            push!(line_y, y)
        end
        push!(vertical_lines, (line_x, line_y))
    end

    # 水平線（y = 定数）
    for y in y_range
        line_x = Float64[]
        line_y = Float64[]
        for x in range(minimum(x_range), maximum(x_range), length=100)
            push!(line_x, x)
            push!(line_y, y)
        end
        push!(horizontal_lines, (line_x, line_y))
    end

    return vertical_lines, horizontal_lines
end

# 格子を変換
function transform_grid(vertical_lines, horizontal_lines, transform_func)
    transformed_vertical = []
    transformed_horizontal = []

    for (xs, ys) in vertical_lines
        new_xs = Float64[]
        new_ys = Float64[]
        for (x, y) in zip(xs, ys)
            z = complex(x, y)
            w = transform_func(z)
            push!(new_xs, real(w))
            push!(new_ys, imag(w))
        end
        push!(transformed_vertical, (new_xs, new_ys))
    end

    for (xs, ys) in horizontal_lines
        new_xs = Float64[]
        new_ys = Float64[]
        for (x, y) in zip(xs, ys)
            z = complex(x, y)
            w = transform_func(z)
            push!(new_xs, real(w))
            push!(new_ys, imag(w))
        end
        push!(transformed_horizontal, (new_xs, new_ys))
    end

    return transformed_vertical, transformed_horizontal
end

# 格子生成
vertical_lines, horizontal_lines = create_grid_lines(x_range, y_range)

# 変換適用
transformed_vertical, transformed_horizontal = transform_grid(
    vertical_lines, horizontal_lines, f)

# プロット作成
fig = Figure(size=(1400, 600))

# 変換前の格子
ax1 = Axis(fig[1, 1],
    title="変換前: z = x + yi",
    xlabel="Real(z)",
    ylabel="Imag(z)",
    aspect=DataAspect())

# 垂直線を描画（青）
for (xs, ys) in vertical_lines
    lines!(ax1, xs, ys, color=:blue, linewidth=1)
end

# 水平線を描画（赤）
for (xs, ys) in horizontal_lines
    lines!(ax1, xs, ys, color=:red, linewidth=1)
end

# 変換後の格子
ax2 = Axis(fig[1, 2],
    title="変換後: w = f(z) = z(z+1)",
    xlabel="Real(w)",
    ylabel="Imag(w)",
    aspect=DataAspect())

# 変換された垂直線を描画（青）
for (xs, ys) in transformed_vertical
    lines!(ax2, xs, ys, color=:blue, linewidth=1)
end

# 変換された水平線を描画（赤）
for (xs, ys) in transformed_horizontal
    lines!(ax2, xs, ys, color=:red, linewidth=1)
end

# PNG画像として保存
save("images/complex_transformation_basic.png", fig)
println("基本の可視化画像を保存しました: images/complex_transformation_basic.png")
```

### コードの解説

#### 1. 初期設定とパッケージ

```julia
using CairoMakie
using LinearAlgebra

set_theme!(fonts = (regular = "Hiragino Sans", bold = "Hiragino Sans"))
```

- **CairoMakie**: 高品質な静的画像を生成するためのプロットライブラリ
- **フォント設定**: Mac環境で日本語を表示するためのフォント指定

#### 2. 変換関数の定義

```julia
f(z) = z * (z + 1)
```

複素数 z に対する変換 f(z) = z² + z を定義。Juliaでは複素数演算が標準でサポートされています。

#### 3. create_grid_lines関数

```julia
function create_grid_lines(x_range, y_range)
    # 垂直線と水平線を生成
end
```

この関数は直交格子を生成します：

- **垂直線（x = 定数）**: 各 x 値に対して、y を100点でサンプリング
- **水平線（y = 定数）**: 各 y 値に対して、x を100点でサンプリング
- `length=100` により滑らかな曲線を描画

#### 4. transform_grid関数

```julia
function transform_grid(vertical_lines, horizontal_lines, transform_func)
    for (x, y) in zip(xs, ys)
        z = complex(x, y)  # 複素数に変換
        w = transform_func(z)  # 変換適用
        push!(new_xs, real(w))  # 実部を取得
        push!(new_ys, imag(w))  # 虚部を取得
    end
end
```

各格子点を複素数として扱い、変換後の実部と虚部を計算します。

#### 5. プロットの作成

```julia
fig = Figure(size=(1400, 600))

ax1 = Axis(fig[1, 1], ...)  # 左側：変換前
ax2 = Axis(fig[1, 2], ...)  # 右側：変換後
```

- **Figure**: 2つのプロットを横に並べるキャンバス
- **Axis**: 各プロット領域（aspect=DataAspect()で縦横比を1:1に）
- **color分け**: 垂直線は青、水平線は赤で区別

## 実行結果

コードを実行すると、以下のような図が得られます：

![複素数変換の基本的な可視化](/images/complex_transformation_basic.png)

- **左側のプロット（変換前）**: 青い垂直線と赤い水平線による直交格子
- **右側のプロット（変換後）**: 直線が放物線や双曲線のような曲線に変換される

特に注目すべき点：

1. **垂直線 (x = 定数) の変換**: 放物線状の曲線になる
2. **水平線 (y = 定数) の変換**: より複雑な曲線を描く
3. **原点付近**: 格子が密に集まり、変換の非線形性が顕著

## より細かい格子での可視化

より詳細に変換を観察したい場合は、格子を細かくします。コード内の `x_range` と `y_range` を以下のように変更することで、格子間隔を0.25に設定できます：

```julia
x_range = -3:0.25:3
y_range = -3:0.25:3
```

![細かい格子での複素数変換の可視化](/images/complex_transformation_fine.png)

細かい格子により、変換の詳細な構造がより明確に見えるようになります。特に臨界点 z = -1/2 付近での変換の様子がより詳しく観察できます。

## まとめ

本記事では、Juliaを使って複素数変換 **f(z) = z(z+1)** を可視化する方法を紹介しました。

### ポイント

1. **複素数変換の数学的性質**
   - 実部・虚部への分解: u = x² - y² + x, v = y(2x + 1)
   - ヤコビ行列と臨界点: z = -1/2 で det(J) = 0
   - 等角写像の性質: 臨界点以外で角度を保つ

2. **CairoMakieによる可視化**
   - 変換前後の格子を並べて表示
   - 色分けにより対応関係を明示
   - 格子の密度を調整して詳細度を変更可能

3. **Juliaの利点**
   - 複素数が言語レベルでサポートされている
   - 簡潔なコードで数学的概念を表現
   - 高品質な数学的可視化が可能

複素数変換の可視化は、複素解析の理解を深める強力なツールです。変換関数 `f(z)` を変更するだけで、様々な複素数関数の幾何学的性質を探求できます。

## 参考資料

- [Julia Documentation - Complex and Rational Numbers](https://docs.julialang.org/en/v1/manual/complex-and-rational-numbers/)
- [Makie.jl Documentation](https://docs.makie.org/stable/)
- [CairoMakie.jl](https://github.com/MakieOrg/Makie.jl/tree/master/CairoMakie)
- [Visual Complex Analysis by Tristan Needham](https://en.wikipedia.org/wiki/Visual_Complex_Analysis)
